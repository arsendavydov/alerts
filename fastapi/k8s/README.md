# Kubernetes

Манифесты развертывания: `k8s-alerts-prod.yaml`, `k8s-alerts-test.yaml`. Включают Deployment, Service, ConfigMap (db_host, db, db_login, db_password, LEVEL_LOG), при необходимости Ingress.
