apiVersion: v1
kind: Service
metadata:
  name: alerts
  namespace: opsm-test
spec:
  type: ClusterIP
  selector:
    app: alerts
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000